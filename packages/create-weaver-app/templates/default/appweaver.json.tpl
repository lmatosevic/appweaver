{
  "$schema": "./node_modules/@appweaver/common/config/schema.json",
  "config": {
    "app": {
      "name": "{{NAME}}",
      "description": "{{DESCRIPTION}}"
    },
    "server": {
      "host": "{{HOST}}",
      "port": {{PORT}}
    },
    "database": {
      "url": "{{DATABASE_URL}}"
    }
  }
}
